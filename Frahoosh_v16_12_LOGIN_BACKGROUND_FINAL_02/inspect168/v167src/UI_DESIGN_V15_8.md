# Frahoosh v15.8 — UI Welcome / Accordion / Theme Update

- Added a typewriter welcome message when entering each role panel.
- Added an operational `وارد پنل شوید` button.
- Panels are collapsed by default; after expansion the button becomes `بستن پنل`.
- Collapsing the panel leaves the compact entry control visible.
- Each role has a dedicated background color in the central dashboard shell.
- The functional panel code and database/data-linking paths were not replaced by decorative screens.
- Student identity behavior remains unchanged: the internal student ID may still exist as a database key, but the student panel does not require a manual ID entry; it resolves the current student from the authenticated account/context.
